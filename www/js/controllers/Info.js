( function ( ) {    'use strict';    /**     * @ngdoc function     * @name app.controller:SignIn     * @author ALEXANDER GUEVARA http://arkeas.com     * @description     * # SignIn     * Controller of the app     */     angular.module('app.info', [ ] );    angular.module( 'app.info' ).controller( 'Info', [ '$scope', '$location', '$rootScope', 'ApiService', Info ] );    function Info( $scope, $location, $rootScope, ApiService ) {        var vm = this;        vm.msg = '';        init( );                function init( ) {            if ( !$rootScope.signedIn ) {                $location.path( '/tabs/signin' );            }           search( );        };                function search( ) {            ApiService.get( 'search', $rootScope.uid )                .then( function ( response ) {                    if ( response.success ) {                        vm.data = response.data;                    } else {                        // Oops something happened                        // But, for now, let it be.                        vm.msg = 'Oops!, Something happened with the google API';                    }                }            );        }        }})();